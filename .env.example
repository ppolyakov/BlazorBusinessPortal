# Local demonstration values only. Choose strong values and never commit a populated .env.
POSTGRES_PASSWORD=replace-with-a-long-local-password
DEMO_PASSWORD=replace-with-a-strong-demo-password
DEMO_RESET_HOUR_UTC=3
WEB_PORT=8080
